<Window x:Class="Game.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="100" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.ColumnSpan="2" Name="Score" Text=" "/>
        <TextBlock Grid.Column="0" Grid.Row="1" Name="Field"/>
        <Grid Grid.Column="1" Grid.Row="1">
            <StackPanel>
                <Button Name="UpButton" Content="Up" />
                <Button Name="DownButton" Content="Down" />
                <Button Name="LeftButton" Content="Left" />
                <Button Name="RightButton" Content="Right" />
                <Button Name="ResetButton" Content="Reset" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>